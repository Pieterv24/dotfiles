[user]
	email = 9167905+Pieterv24@users.noreply.github.com
	name = Pieter
{{- if .git_signing.enabled }}
	signingkey = {{ .git_signing.key }}
{{- end }}

[init]
	defaultBranch = master

[color]
	ui = auto

[pull]
	rebase = false

[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process

{{ if .git_signing.enabled -}}
[commit]
	gpgsign = true
[gpg]
	program = gpg2
{{- end }}
